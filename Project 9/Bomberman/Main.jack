class Main {
    function void main() {
		var int yMax, yMin;
		var Game game;
		let yMin = 16*2;
		let yMax = 256-(yMin+16);
		let game = Game.new(30, 1, yMax, yMin);
		do game.run();
		do game.dispose();
        return;
    }
}